<nav class="navbar navbar-expand-md navbar-dark bg-info navbar-fixed-top offset-7 col-5">
  <a class="navbar-brand" href="#">英雄検索navi</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <ul class="navbar-nav">
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          名前一覧
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
          <a class="dropdown-item" href="#">
            <% @characters.each do |character| %>
              <a href="/equipments#<%= character.id %>"><%= character.name %></a></br>
            <% end %>
          </a>
        </div>
      </li>
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          アビリティ絞り込み
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
          <a class="dropdown-item" href="#">
              <%= form_tag(equipments_path, method: "get") do %>
              <%= select_tag(:name, @ability) %>
              <%= submit_tag "絞り込み" %>
            <% end %>
            
            <% @ability.each do |f| %>
              <p><%= f %></p>
            <% end %>
          </a>
        </div>
      </li>
      <li class="nav-item">
          <a class="nav-link" href="#">検索フォーム予定</a>
      </li>
    </ul>
  </div>
</nav>


<div class="table table-bordered">
  <% if user_signed_in? %>
    <div class="user_nav grid-6">
      <%= link_to 'キャラ投稿へ', "/characters/new", method: :get, class: 'btn' %>
    </div>
  <% end %>
</div>

<div class="conetainer">
  <div class="row">
  <div class="col-6">
    <div class="row">
      <% @characters.each do |character| %>
        <div class="col-6">
        <div class="panel panel-default col-auto col-offset-1">
          <div id="name" class="panel-heading lead col-auto"><%= character.name %></div>
          <div class="panel-body">
            <p>アビリティ</p>
            <% character.chara_abis.each do |characters_abilitys| %>
            <span><%= characters_abilitys.ability.name %></span>
            <span><%= characters_abilitys.valume %></span>
            <% end %>
            <div class="panel-footer">
              <% if user_signed_in? %>
                <%= link_to '編集', "/characters/#{character.id}/edit", method: :get %>
                <%= link_to '削除', "/characters/#{character.id}", method: :delete %>
              <% end %> 
            </div>  
          </div>      
        </div>
        </div>
      <% end %>
    </div>
  </div>
  </div>
</div>