<div class="nested-fields">
  <%= f.collection_select(:ability_id, Ability.all, :id, :name, :include_blank => true) %>
  <%= f.text_field :valume, html: {class: 'form-control'} %>
  <%= link_to_remove_association "削除", f %>
</div>

